import 'package:extility/extility.dart';

void main() {
  print('[1].bracket(() => 0');
  print([1].bracket(() => 0));

  print('[1, 2, 3].interleave(() => 0)');
  print([1, 2, 3].interleave(() => 0));
}
